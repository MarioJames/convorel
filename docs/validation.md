# Validation — 2026-09-17

Environment: Linux x86_64, Bun 1.4.2, Node 24.21.0, Git 2.47.3, installed Google Chrome, packaged agent-browser 0.34.0. No hosted service or published npm package is assumed.

| Layer                         | Result               | Evidence and limits                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TypeScript + behavior tests   | Pass                 | 38 tests: ignore/path/byte limits, Git rename filtering and execution suppression, writer ownership/recovery, request idempotency, uncertain send reconciliation, exact result retention, draft/navigation/epoch protection, model selection, organization, installer conflict preservation and repeated initialization.                                                                                                       |
| SDK stdio MCP                 | Pass                 | A real SDK client spawns the CLI, discovers all six tools and reads allowed fixture files; secret/path errors are verified. The child is closed afterward.                                                                                                                                                                                                                                                                     |
| Real Chrome                   | Pass                 | Disposable local profile: list/attach/rebind creates no implicit blanks; one explicit new tab changes 1 → 2 → 1; missing pinned target fails without fallback. ProseMirror paragraph extraction preserves prompt line breaks. Test Chrome and profile are released.                                                                                                                                                            |
| Source bootstrap              | Pass                 | Locked local dependencies, private workspace config, repeatable skill symlink installation, loopback CDP connection and all six MCP tools checked. No global toolchain installation.                                                                                                                                                                                                                                           |
| Packaged installation         | Pass                 | Local tarball installed into a path containing spaces; CLI and installed skill wrapper run outside the project; packaged agent-browser executes; MCP returns fixture evidence. Temporary package tree and subprocesses are released.                                                                                                                                                                                           |
| ChatGPT web                   | Pass, bounded scope  | Architecture reviewed in 6 Pro. New CLI imported the completed review, sent a naming follow-up, recovered/read exact replies and verified title/project after reload. The first naming submission exposed a paragraph-extraction mismatch before Send; it was corrected and submitted once. A later naming follow-up completed through the normal CLI path. Remote opinion does not establish source access or test execution. |
| Native tunnel-client          | Local preflight only | Official v0.0.14 Linux archive SHA-256 verified; native doctor accepted the generated flags and reported the missing runtime API key. No cloud daemon was started. Native binary is a local ignored artifact, not redistributed in the package.                                                                                                                                                                                |
| ChatGPT → tunnel → local code | Not run              | Operator supplied a tunnel ID but has not configured a runtime key/developer connection. Local MCP success is not cloud end-to-end acceptance.                                                                                                                                                                                                                                                                                 |

The ChatGPT conversation URL, exact message/run IDs, full replies and cleanup evidence are retained in private local state outside the repository. Temporary UI reload errors occurred during organization; a subsequent reload verified both actual title and project. An older external monitor lost its global agent-browser executable; switching that monitor to the project's pinned dependency recovered the same completed turn without resending. Convorel itself resolves the packaged executable and does not require a global agent-browser.

The creating agent closed its diagnostic conversation target after saving the reply and rechecking the current message/hash, draft and generation state. The imported task correctly treated that tab as borrowed and declined to close it itself. Final shared-browser count returned to the one pre-existing blank tab; Chrome and login data remain. The monitoring terminal was closed. No app dev server, tunnel daemon, database or public ingress was created. Shared browser console/network health is not asserted globally; this is a CLI with a third-party web adapter, not a frontend application release.

Reproduce locally:

```bash
bun run check
bun run format:check
bun run test:browser --chrome /path/to/installed/chrome
bun run test:package
```

Name selection: ChatGPT proposed Convorel (conversation + relay). On this date, the npm registry returned 404 for the exact name, GitHub repository-name search showed no exact match, and web search showed no obvious same-name software. These are limited availability checks, not a name reservation or trademark clearance.
